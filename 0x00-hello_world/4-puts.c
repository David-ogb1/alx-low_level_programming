#include <stdio.h>

/**
 * main - Entry point
 * Description: C programm that prints a functionfollowed by a new line
 * Return: 0 if successful
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
